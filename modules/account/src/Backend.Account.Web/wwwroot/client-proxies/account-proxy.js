/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module account

(function(){

  // controller backend.account.account

  (function(){

    abp.utils.createNamespace(window, 'backend.account.account');

    backend.account.account.register = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/register',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    backend.account.account.sendPasswordResetCode = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/send-password-reset-code',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    backend.account.account.verifyPasswordResetToken = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/verify-password-reset-token',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    backend.account.account.resetPassword = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/reset-password',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(input)
      }, ajaxParams));
    };

  })();

  // controller backend.account.web.areas.account.controllers.account

  (function(){

    abp.utils.createNamespace(window, 'backend.account.web.areas.account.controllers.account');

    backend.account.web.areas.account.controllers.account.login = function(login, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/login',
        type: 'POST',
        data: JSON.stringify(login)
      }, ajaxParams));
    };

    backend.account.web.areas.account.controllers.account.logout = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/logout',
        type: 'GET',
        dataType: null
      }, ajaxParams));
    };

    backend.account.web.areas.account.controllers.account.checkPassword = function(login, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/check-password',
        type: 'POST',
        data: JSON.stringify(login)
      }, ajaxParams));
    };

  })();

  // controller backend.account.profile

  (function(){

    abp.utils.createNamespace(window, 'backend.account.profile');

    backend.account.profile.get = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/my-profile',
        type: 'GET'
      }, ajaxParams));
    };

    backend.account.profile.update = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/my-profile',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    backend.account.profile.changePassword = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/account/my-profile/change-password',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(input)
      }, ajaxParams));
    };

  })();

})();


